plugins {
    id 'java'
}

apply from: rootProject.file('gradle/common-domain.gradle')

group = 'com.tonyjev93.blogsearch'
version = '0.0.1-SNAPSHOT'
sourceCompatibility = '17'

repositories {
    mavenCentral()
}

tasks.named('test') {
    useJUnitPlatform()
}
